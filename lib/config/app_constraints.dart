// networking
const Duration connectTimeout = Duration(seconds: 10);
const Duration sendTimeout = Duration(seconds: 10);
const Duration receiveTimeout = Duration(seconds: 10);
const int itensPerPage = 20;

// Snack
const double snackbarOverlayBlur = 2;

// validation
const int userNameMinLenght = 3;
const int contactNameMinLenght = 3;